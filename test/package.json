{
	"name": "mobilecli-tests",
	"version": "1.0.0",
	"description": "E2E tests for MobileCLI server",
	"main": "index.ts",
	"scripts": {
		"test": "playwright test",
		"test:server": "playwright test --project=server",
		"test:ios": "playwright test --project=simulator",
		"test:ios:app": "playwright test --project=ios-app",
		"test:simulator": "playwright test --project=simulator",
		"test:android": "playwright test --project=emulator"
	},
	"author": "",
	"license": "ISC",
	"type": "commonjs",
	"devDependencies": {
		"@playwright/test": "^1.49.0",
		"@types/node": "^24.3.0",
		"typescript": "^5.9.2"
	},
	"overrides": {
		"serialize-javascript": "^7.0.3"
	}
}
